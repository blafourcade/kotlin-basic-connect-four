// You can experiment here, it won’t be checked

fun main(args: Array<String>) {
    val string: String = "ttt"
    println(string.length > 0)
    println(string.length == 1)
    println(string.isNotEmpty())
    println(string.lastIndex >= 0)
}
